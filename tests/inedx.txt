// import { describe, it, expect, vi, beforeEach } from "vitest";
// import { Menu } from "../src/ui/Menu.js";

// describe("Main Entry Point", () => {
//   beforeEach(() => {
//     vi.resetModules();
//     vi.clearAllMocks();
//     // Espiamos el método iniciar del prototipo para que afecte a CUALQUIER instancia
//     vi.spyOn(Menu.prototype, "iniciar").mockResolvedValue(undefined);
//   });

//   it("Debe instanciar el Menu y llamar a iniciar", async () => {
//     const spy = vi.spyOn(Menu.prototype, "iniciar");

//     // Importar el archivo provoca la ejecución del main()
//     await import("../src/index.js");

//     // Verificamos que el método del prototipo fue llamado
//     expect(spy).toHaveBeenCalled();
//   });

//   it("Debe capturar y mostrar errores si el inicio falla", async () => {
//     const consoleSpy = vi.spyOn(console, "error").mockImplementation(() => {});
    
//     // Forzamos que el próximo inicio falle
//     vi.spyOn(Menu.prototype, "iniciar").mockRejectedValueOnce(new Error("Portal Gun sin batería"));

//     await import("../src/index.js");

//     // Pequeña pausa para que el catch() asíncrono se ejecute
//     await new Promise(resolve => setTimeout(resolve, 0));

//     expect(consoleSpy).toHaveBeenCalledWith(
//       "Error al iniciar la aplicación:",
//       expect.any(Error)
//     );

//     consoleSpy.mockRestore();
//   });
// });